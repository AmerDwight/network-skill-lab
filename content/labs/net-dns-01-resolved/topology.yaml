nodes:
  web01: { role: ubuntu }
  dns01: { role: ubuntu }
links:
  - endpoints: ["web01:eth1", "dns01:eth1"]
    subnet: "{{subnet}}"
    addresses: { web01: "{{ip_web}}/24", dns01: "{{ip_dns}}/24" }
